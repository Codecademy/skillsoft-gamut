# Changesets

This directory holds the changeset files that drive version bumps and changelogs.

Run `yarn changeset` after changing a published package, and commit the file it writes.
See [CONTRIBUTING.md](../CONTRIBUTING.md) for when a changeset is required, how the fixed group works, and how to skip a release with `--empty`.
