{
  "name": "gamut-styles",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/gamut-styles/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "outputs": ["{projectRoot}/dist"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/gamut-styles",
        "commands": [
          "rm -rf ./dist",
          "tsdown",
          "tsc --emitDeclarationOnly --project tsconfig.lib.json",
          "cpy './src/**/*.{d.ts,svg,png}' ./dist"
        ],
        "parallel": false
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/coverage/packages/gamut-styles"],
      "options": {
        "jestConfig": "packages/gamut-styles/jest.config.ts",
        "passWithNoTests": false
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "rm -rf ./dist"
      }
    },
    "verify-package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["publint .", "attw --pack . --ignore-rules false-cjs"],
        "parallel": false
      }
    },
    "publish-build": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "commands": [],
        "parallel": false
      }
    }
  }
}
